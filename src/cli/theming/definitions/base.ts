import { BaseThemeDefinition } from "../../../themes/_generator/tokens/types";

const base: BaseThemeDefinition = {
	viewport: {
		s: { maxPx: 659 },
		m: { minPx: 660 },
		l: { minPx: 900 },
		xl: { minPx: 1280 },
	},
};

export default base;
