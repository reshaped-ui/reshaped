---
"reshaped": minor
---

ScrollArea: Added an `orientation` property (`"vertical" | "horizontal" | "both"`) that limits scrolling to the passed direction and constrains the content size in the other direction, defaulting to `"both"`
