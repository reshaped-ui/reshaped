---
"reshaped": minor
---

DropdownMenu, ContextMenu: Added a `size` property to the root components. It's used as the default size of all their menu items, including the items of the nested submenus, and can still be overridden per item.
