---
"reshaped": minor
---

DropdownMenu: Added the `size` property which is used by all menu items and nested submenus by default. ContextMenu inherits the same property, and individual `DropdownMenu.Item` components can still override it.
